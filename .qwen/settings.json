{
  "mcpServers": {
    "xezar": {
      "command": "npx",
      "args": ["-y", "@qodeca/xezar", "mcp"],
      "description": "Xezar cockpit bridge (cockpit on :4321). Stdio, started per session in this project; leader_events attaches this session as the project leader. One client owns the leader connection at a time (AGENTS.md).",
      "discoveryTimeoutMs": 30000
    }
  },
  "$version": 4
}
